__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Generated screenshots from the example scripts (regenerable via make demo_tiers / make render)
examples/screenshots/*.png

# Agent scratch memory — per-session, never shared state
MEMORY.md

# Ephemeral local artifacts — never tracked. Put logs, PR-body drafts and throwaway
# scripts under .scratch/ (CI logs, validate output, payload tests, ...).
*.log
.scratch/
