"""Opt-in contrib extras. Nothing in core may import from this package.

Contrib modules are unsupported extras: they consume the public
``polyfetch_scrape.fetch`` API but core behaviour never depends on them.
"""
